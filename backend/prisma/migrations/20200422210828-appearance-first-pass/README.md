# Migration `20200422210828-appearance-first-pass`

This migration has been generated by Robert Westbury at 4/22/2020, 9:08:28 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."Appearance" ADD COLUMN "successBackgroundColor" text  NOT NULL DEFAULT '#fafafa',
ADD COLUMN "successButtonColor" text  NOT NULL DEFAULT '#057a55',
ADD COLUMN "successCustomRedirect" text  NOT NULL DEFAULT '',
ADD COLUMN "successDots" boolean  NOT NULL DEFAULT true,
ADD COLUMN "successText" text  NOT NULL DEFAULT 'Your form has successfully been recieved and processed. Thank you!',
ADD COLUMN "successTickBackground" text  NOT NULL DEFAULT '#def7ec',
ADD COLUMN "successTickColor" text  NOT NULL DEFAULT '#057a55';
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200421190349-add-appearence..20200422210828-appearance-first-pass
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 generator client {
   provider      = "prisma-client-js"
@@ -44,11 +44,19 @@
   appearance    Appearance
 }
 model Appearance {
-  id     Int    @default(autoincrement()) @id
-  formId String
-  form   Form   @relation(fields: [formId], references: [id])
+  id                     Int     @default(autoincrement()) @id
+  formId                 String
+  form                   Form    @relation(fields: [formId], references: [id])
+  // Success
+  successCustomRedirect  String  @default("")
+  successTickBackground  String  @default("#def7ec")
+  successTickColor       String  @default("#057a55")
+  successText            String  @default("Your form has successfully been recieved and processed. Thank you!")
+  successButtonColor     String  @default("#057a55")
+  successBackgroundColor String  @default("#fafafa")
+  successDots            Boolean @default(true)
 }
 model Confirmation {
   id          Int    @default(autoincrement()) @id
```


