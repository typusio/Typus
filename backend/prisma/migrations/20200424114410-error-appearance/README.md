# Migration `20200424114410-error-appearance`

This migration has been generated by Robert Westbury at 4/24/2020, 11:44:10 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."Appearance" ADD COLUMN "errorBackgroundColor" text  NOT NULL DEFAULT '#fafafa',
ADD COLUMN "errorButtonColor" text  NOT NULL DEFAULT '#e02424',
ADD COLUMN "errorCustomRedirect" text  NOT NULL DEFAULT '',
ADD COLUMN "errorDots" boolean  NOT NULL DEFAULT true,
ADD COLUMN "errorIconBackground" text  NOT NULL DEFAULT '#fde8e8',
ADD COLUMN "errorIconColor" text  NOT NULL DEFAULT '#e02424',
ADD COLUMN "errorMode" text  NOT NULL DEFAULT 'Our';
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200423134720-appearance-mode..20200424114410-error-appearance
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 generator client {
   provider      = "prisma-client-js"
@@ -56,8 +56,16 @@
   successText            String  @default("Your form has successfully been recieved and processed. Thank you!")
   successButtonColor     String  @default("#057a55")
   successBackgroundColor String  @default("#fafafa")
   successDots            Boolean @default(true)
+  // Error
+  errorMode              String  @default("Our")
+  errorCustomRedirect    String  @default("")
+  errorIconBackground    String  @default("#fde8e8")
+  errorIconColor         String  @default("#e02424")
+  errorButtonColor       String  @default("#e02424")
+  errorBackgroundColor   String  @default("#fafafa")
+  errorDots              Boolean @default(true)
 }
 model Confirmation {
   id          Int    @default(autoincrement()) @id
```


